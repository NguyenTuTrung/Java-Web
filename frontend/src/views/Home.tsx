import { Button, Select } from "@/components/ui"
import { Fragment } from "react"

const Home = () => {
    return (
        <Fragment>
            <Button size="sm">Hello word</Button>
        </Fragment>
    )
}

export default Home
